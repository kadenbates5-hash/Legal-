# Used by docker-compose.yml. Copy to .env and fill in real values —
# .env is gitignored, never commit real credentials.
POSTGRES_PASSWORD=change-me
ATTORNEY_USERNAME=you
ATTORNEY_PASSWORD=at-least-8-characters
